import { Injectable } from '@nestjs/common';
import { FlowPrams } from 'src/shared/params/flow.params';

@Injectable()
export class FlowService {
  async createFlow(userId: FlowPrams): Promise<any>{

    
  }
}
